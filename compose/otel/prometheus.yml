# Scrapes what the collector converted from OTLP. One job: the lab is one stack.
global:
  scrape_interval: 5s
  evaluation_interval: 15s

scrape_configs:
  - job_name: maf-lab
    static_configs:
      - targets: ["otel-collector:8889"]
