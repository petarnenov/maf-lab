# Orphan notes

## Purpose

These notes were dropped into the corpus without a tenant folder. The owning firm cannot be determined from the path, so the indexer must reject this file instead of guessing a tenant or defaulting to shared. It exists only to exercise that rejection path.

## Content

Draft thoughts on moving billing runs from quarterly to monthly cadence, including the impact on proration, the extra custodian fee deduction files, and how breakpoints are evaluated when household AUM is sampled more often. No firm has approved this change and it should never appear in any tenant's answers.
